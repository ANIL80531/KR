#include<stdio.h>	//	code/library which other wrote for our use.
int main(){
	putchar('\n');
	// main is a special function every program start executing from main.
	printf("Hello, World!\n");	// printf is function from stdio.h library.
	//	arguments are used to communicate data between functions.
	//	arguments are surrounded by parenthesis after function.
	//	empty parenthesis after a function means it accepts no argument.
	//	\n is new line character.
	//	the body of function which contains statements is enclosed in {}
	//	A function is called by using its name and followed by arguments in parenthesis.
	//	Characters in double quotes are string constant.
	







	putchar('\n');
	putchar('\n');
	putchar('\n');
}
